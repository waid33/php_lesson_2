Домашнее задание: 
А) Поделать все задачки отсюда. Решаем самым оптимальным способом. Где можно функцию применить - применяем. №27 делаем со случайным цветом. https://github.com/henry2778/groupTueThu/tree/master/loops - можете глянуть тут подсказку

Все задачи оформить так чтобы на страничке было отображено задание, решение и ответ. Отдельная -адача - одтельный файл. Или можно сгруппировать, но так чтоб на страничке было видно где какой номер

В) дополнительные задачи:

1. Составить список случайной длины (-20 до 20) из случайных чисел в диапазоне от -100 до 100.  Подсчитать количество четных, нечетных, положительных и отрицательных значений. Составить ассоциативный массив их этих значений

2. Дано случайное число. Определить простое ли оно (загуглите что это, если не помните)

3. Дан текст. Определить какая из двух заданных букв встечается чаще.

4. Определить, правильно ли в тексте расставлены круглые скобки

5. Определить является ли строка правильное записью целого числа

6. Подсчитать в тексте максимальное количетсво цифр, идущих подряд

7. Проверить строку на симметричность
Вариант продвинутых прогеров с алгоритмом(потом разобрать)
$max = 0;
$len = strlen($s);
for ($i = 0 ; $i < $len - 1 ; $i++)    /// O(n)
{
    /// проверка нечетной длины  O(n)
    for ($j = 0 ; $i - $j >=0 && $i + $j < $len && $s[$i - $j]==$s[$i + $j] ; $j++);
    $max = max($max,($j-1)*2+1);

    /// проверка четной длины  O(n)
    for ($j = 0 ; $i - $j >=0 && $i + 1 + $j < $len && $s[$i - $j]==$s[$i + 1 + $j] ; $j++);
    $max = max($max,$j*2);
}

С) читать теорию - на пхп.нет про строки и массивы, глянуть примеры там